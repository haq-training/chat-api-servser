overwrite: true
schema: 'http://localhost:4000'
documents: null
config:
  typesPrefix: I
generates:
  src/__generated__/graphql.ts:
    plugins:
      - 'typescript'
      - 'typescript-resolvers'
    config:
      mappers:
        User: ../db_models/init-models#user
